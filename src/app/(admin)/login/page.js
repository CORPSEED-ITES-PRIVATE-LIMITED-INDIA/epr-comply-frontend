import Login from "@/components/admin/Login";

export const metadata = {
  title: "Admin Login | EPR Comply",
  robots: { index: false, follow: false },
};

export default function LoginPage() {
  return <Login />;
}
